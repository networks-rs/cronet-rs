// OpenHarmony uses musl. Keep Chromium's generated libc++ configuration and
// override only the libc family selected by this target.
#include <__config_site_upstream>

#if defined(__OHOS__)
#undef _LIBCPP_HAS_MUSL_LIBC
#define _LIBCPP_HAS_MUSL_LIBC 1
#endif
